if test ! -x "$PYENV_COMMAND_PATH"; then {
    for bindir in "${GP_PYENV_MIRROR}/versions/${PYENV_VERSION}/lib/python${PYENV_VERSION%.*}/site-packages/bin/$PYENV_COMMAND"
    GP_PYENV_MIRROR="/workspace/.pyenv";
    PYENV_COMMAND_PATH=
} fi